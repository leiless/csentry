/*
 * Created 190731 lynnl
 *
 * Constants written by CMake
 */

#include <sys/param.h>

#define CONST_HOSTNAME                  "${CONST_HOSTNAME}"

/* contexts:app */
#define CONST_CMAKE_BUILD_TYPE          "${CMAKE_BUILD_TYPE}"
#define CONST_CMAKE_C_FLAGS             "${CMAKE_C_FLAGS}"
#define CONST_COMPILE_DEFINITIONS       "${COMPILE_DEFINITIONS}"
#define CONST_PTR_BITS                  (${CMAKE_SIZEOF_VOID_P}u << 3u)

/* contexts:os */
#define CONST_CMAKE_SYSTEM_NAME         "${CMAKE_SYSTEM_NAME}"
#define CONST_CMAKE_SYSTEM_VERSION      "${CMAKE_SYSTEM_VERSION}"
#define CONST_OS_RELEASE                "${CONST_OS_RELEASE}"
#define CONST_OS_VERSION                "${CONST_OS_VERSION}"

/* contexts:device */
#define CONST_CMAKE_SYSTEM_PROCESSOR    "${CMAKE_SYSTEM_PROCESSOR}"

/* see: https://sourceforge.net/p/predef/wiki/OperatingSystems */
#if defined(__unix__) || defined(__unix) || defined(BSD)
#define CONST_UNAME                     "${CONST_UNAME}"
#endif

#ifdef __APPLE__
#define CONST_HW_MODEL                  "${CONST_HW_MODEL}"
#endif

#define CONST_PHYS_MEM_TOTAL            (${CONST_PHYS_MEM_TOTAL} * 1048576ULL)
#define CONST_PHYS_MEM_FREE             (${CONST_PHYS_MEM_FREE} * 1048576ULL)
#define CONST_PHYS_CORES                (${CONST_PHYS_CORES})
#define CONST_LOGI_CORES                (${CONST_LOGI_CORES})

